<!doctype html>
<html lang="en">
<head>
  {{> head }}
</head>
<body>
  {{> hcard-basics additionalClasses="p-author" }}
  <header>
    <h1><a href="/">Qubyte Codes</a></h1>
    {{> nav }}
  </header>
  <main>
    <p>This is a collection of sites with RSS feeds I subscribe to. You can download this collection to import into your feed reader as an <a href="/feeds.opml">OPML file</a>.</p>
    <dl>
      {{#feeds}}
      <div class="h-card" light-emoji="{{lightEmoji}}" dark-emoji="{{darkEmoji}}">
        <dt><a href="{{htmlUrl}}" class="u-url p-name">{{name}}</a></dt>
        <dd class="p-note">{{description}}</dd>
      </div>
      {{/feeds}}
    </dl>
  </main>
  <footer>
    {{> copyright }}
  </footer>
  </div>
</body>
</html>
