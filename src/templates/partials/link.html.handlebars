<article class="h-entry" light-emoji="{{lightEmoji}}" dark-emoji="{{darkEmoji}}">
  {{> hcard-basics additionalClasses="p-author" }}
  <header>
    <a href="/links/{{timestamp}}"><time datetime="{{datetime}}" class="dt-published">{{humanDateTime datetime}}</time></a>
  </header>
  {{#if bookmarkOf}}
  <p class="e-content">
    {{content}}
    {{#if syndications.twitter}}<a class="mention-of" href="https://brid.gy/publish/twitter"></a>{{/if}}
    {{#if syndications.mastodon}}<a class="mention-of" href="https://brid.gy/publish/mastodon"></a>{{/if}}
  </p>
  <a class="u-bookmark-of p-name" href="{{bookmarkOf}}">{{name}}</a>
  {{else if repostOf}}
  <a class="u-repost-of p-name" href="{{repostOf}}">{{name}}</a>
  {{/if}}
</article>
